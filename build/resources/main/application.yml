spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format: true
  datasource:
    url: jdbc:h2:mem:bluedb
    username: blueground
    password: bluepass
    platform: org.hibernate.dialect.H2Dialect
    driverClassName: org.h2.Driver
server:
  port: 8090