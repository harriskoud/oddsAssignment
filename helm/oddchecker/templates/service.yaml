apiVersion: v1
kind: Service
metadata:
  name: {{ include "oddchecker.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "oddchecker.name" . }}
    helm.sh/chart: {{ include "oddchecker.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "oddchecker.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}